R-TeleSwiftBot Modified Package
================================

Run:
  1. Fill in config.env, or set environment variables/secrets.
  2. Install dependencies: python -m pip install -r requirements.txt
  3. Start: python bot.py

main.py is a compatibility launcher for bot.py.
config.env contains placeholders only. Do not commit real credentials.
