bgutil-ytdlp-pot-provider>=1.3.1
cryptography>=50.0.0
kurigram>=2.2.24
pymongo>=4.13.0
py-tgcalls>=2.3.3
python-dotenv>=1.2.2
redis>=8.1.0
tgcrypto>=1.2.5
# The [default] extra pulls in the yt-dlp-ejs version yt-dlp itself is
# pinned to for that release. yt-dlp rejects any yt-dlp-ejs build that
# doesn't match, so yt-dlp-ejs must NOT be pinned as an independent line
# here — an independently resolved ">=" floor can easily land on a version
# yt-dlp refuses, silently breaking YouTube downloads after a rebuild.
yt-dlp[default]>=2026.7.4